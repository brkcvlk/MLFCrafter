name: 🔖 Bump Version & Create PR

on:
  workflow_dispatch:
    inputs:
      version_type:
        description: 'Version bump type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  pull-requests: write

jobs:
  bump-version:
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.get_new_version.outputs.NEW_VERSION }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install bump2version
        run: pip install bump2version

      - name: Get old version
        id: get_old_version
        run: |
          OLD_VERSION=$(python -c "import mlfcrafter; print(mlfcrafter.__version__)")
          echo "OLD_VERSION=$OLD_VERSION" >> $GITHUB_OUTPUT
          echo "Old version: $OLD_VERSION"

      - name: Bump version
        run: |
          bump2version ${{ inputs.version_type }} --allow-dirty --no-tag
        id: bump

      - name: Get new version
        id: get_new_version
        run: |
          NEW_VERSION=$(python -c "import mlfcrafter; print(mlfcrafter.__version__)")
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version: $NEW_VERSION"

      - name: Create bump branch and push
        run: |
          BRANCH_NAME="bump-v${{ steps.get_new_version.outputs.NEW_VERSION }}"
          git checkout -b $BRANCH_NAME
          git push --set-upstream origin $BRANCH_NAME

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: bump-v${{ steps.get_new_version.outputs.NEW_VERSION }}
          title: "🔖 Bump version to v${{ steps.get_new_version.outputs.NEW_VERSION }}"
          body: |
            This PR bumps version from ${{ steps.get_old_version.outputs.OLD_VERSION }} to ${{ steps.get_new_version.outputs.NEW_VERSION }}.
          base: main
          delete-branch: true
